@model PagedList.IPagedList<dvhd.Models.News>
@using PagedList.Mvc; 
@{
    ViewBag.Title = "Danh sách tin tức";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var news = "";
    var link = "";
}
<div style="float:right;display:block;position:relative;width:100%;margin-left:20px;"><a href="/News/Create"><img src="/Images/Create.png" width="32" height="32">Gửi bài</a></div>
<div class="container" style="width:100%;">
  <div class="row" style="width:100%;"> 
    <!-- Blog -->
    <section class="blog mt50">
      <div class="col-md-12" style="width:100%;">
         @foreach (var item in Model) {
             link = "/News/Details/"+ item.id;
             ///hotel/" + Config.unicodeToNoMark(prs[j].name) + "-" + ViewBag.fromdate + "-" + ViewBag.todate + "-" + prs[j].id + "
             news += "<div class=\"col-sm-3 single\" style=\"height:auto;display:block;position:relative;float:left;margin-top:5px;padding-top:5px;\">";
             news += " <div ><a href=\"" + link + "\"><img src=\"" + item.image + "\" alt=\"" + item.title + "\"  style=\"width:250px;height:200px;\"/></a>";
             news += "  <div class=\"mask\">";
             news += "   <div class=\"main\">";
             news += "      <a href=\"" + link + "\"><b>" + item.title + "</b></a>";
             //news += "      <p style=\"margin:2 2 2 2;\">" + item.des + "</p>";
             news += "    </div>";
             news += " </div>";
             news += " </div>";
             news += " </div>";
         }    
         @Html.Raw(@news)  
      </div>
      @if (Model.PageCount>1){
                <div class="col-md-9" style="float:right;">
                <ul class="pagination clearfix">
                        <li class="disabled"><a href="#">«</a></li>
                        @for (int i = 1; i <= Model.PageCount; i++)
                        {
                            if (i != ViewBag.page){
                               <text><li><a href="/News/List?page=@i">@i</a></li></text>
                            }else{
                               <text><li><a href="/News/List?page=@i" style="background-color:#8a6d3b;">@i</a></li></text>
                            }
                        }
                </ul>
                </div>
            } 
    </section>
   </div>
</div>
